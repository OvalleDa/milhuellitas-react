import React from 'react'

function TitleBottom({ title, text }) {
  return (
    <section className="titleSection">
      <h1>{title}</h1>
      <p>{text}</p>
    </section>
  )
}

export default TitleBottom
